{% extends 'base.html.twig' %}

{% block nav %}
    <li><a class="nav-link" href="{{ path('app_logout') }}">Logout</a></li>
{% endblock %}

{% block body %}
    <div class="container">
    <div class="row">
    <div class="col-md-10 ml-md-auto">
        <div class="">
            <div class="card mb-3 mt-5" style="width: 800px;">
                <div class="card-body">
    {% for flashSuccess in app.flashes('success') %}
        <div class="alert alert-success" role="alert">{{ flashSuccess }}</div>
    {% endfor %}

    <h1 class="h3 mb-3 font-weight-normal">Welcome, {{ app.user.username }}</h1>
                </div>
            </div>
        </div>
    </div>
    </div>
    </div>
{% endblock %}